/**
 * api-specs/02-authentication.md 2.1. The `__Host-` prefix forbids a `Domain`
 * attribute, so each tenant host holds its own session.
 */
export const SESSION_COOKIE_NAME = "__Host-archiva_session";
