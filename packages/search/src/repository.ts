/**
 * This module owns no tables. It owns the OpenSearch index and its mapping, so
 * its persistence seam is the SearchIndex port rather than a repository.
 * technical-specs/05-module-definitions.md 5.6.
 */
export type {};
